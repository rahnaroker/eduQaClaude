# Шпаргалка

Краткие выжимки по пройденным темам. Формат: определение → нюансы → ловушки.

---

## Java Core

### 1.1 OOP (в процессе)

**Overloading vs Overriding**
- Overloading — тот же класс, разные сигнатуры, resolved в compile-time
- Overriding — дочерний класс, та же сигнатура, resolved в runtime
- Ловушка: overloading с `null` — компилятор выбирает наиболее специфичный тип

**Static dispatch vs Dynamic dispatch**
- Static dispatch: компилятор вписывает конкретный метод в байткод (`invokestatic`) — overloading, static методы
- Dynamic dispatch: JVM смотрит на реальный тип объекта в рантайме через vtable (`invokevirtual`) — overriding
- vtable — таблица виртуальных методов, строится JVM для каждого класса

**Static метод в дочернем классе**
- Переопределить нельзя — нет dynamic dispatch
- Можно объявить с той же сигнатурой — это **method hiding**
- Вызов идёт по типу переменной, не объекта — полиморфизма нет

**Abstract class vs Interface**

| | Abstract class | Interface |
|---|---|---|
| Поля с состоянием | ✅ | ❌ (только `static final`) |
| Конструктор | ✅ | ❌ |
| Множественное наследование | ❌ | ✅ |
| `default` методы | ✅ | ✅ (Java 8+) |

- Abstract class — когда есть общее состояние и частичная реализация (пример: `BaseTest`)
- Interface — когда нужен чистый контракт без привязки к иерархии
- Ловушка: abstract class может не содержать ни одного abstract метода — это валидно

**Контракт**
- Соглашение о том, ЧТО объект умеет делать, без указания КАК
- Интерфейс = чистый контракт; abstract class = контракт + частичная реализация
- "Программируй на интерфейсы" — зависеть от контракта, а не от конкретного класса

---

## Автоматизация тестирования

_темы появятся по мере прохождения_

---

## Нагрузочное тестирование

_темы появятся по мере прохождения_

---

## Системный дизайн и процессы

_темы появятся по мере прохождения_

---
